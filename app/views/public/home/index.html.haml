- content_for :head do
  %meta{content: "no-cache", name: "turbolinks-cache-control"}
.container
  .main-body
    .body-inner-content
      %strong> New Features: You can now embed E-Library books on your website!
      %a.learn-more{:role => "button"} Learn More
    .all-margin
      .col-md-12
        .Classic-Books
          .browse-subject
            %a Browse By Subject
          .scroller
            - @subjects.each do |book|
              .col-sm-4
                .single-item
                  .img-cover
                    %img.img-responsive.joseph
                      = link_to(image_tag(url_for(book.images), class: 'image-list'), public_book_path(book))
      .col-md-12
        .Classic-Books
          .browse-subject
            %a Classic Books
          .scroller
            - @books.each do |book|
              .col-sm-4
                .single-item
                  .img-cover
                    %img.img-responsive.joseph
                      = link_to(image_tag(url_for(book.images), class: 'image-list'), public_book_path(book))
                    %centre
                      = link_to 'Read More', public_book_path(book), 
                        role: 'button', class: 'read-more'

      .col-md-12
        .Classic-Books
          .browse-subject
            %a Romance
          .scroller
            - @books.each do |book|
              .col-sm-4
                .single-item
                  .img-cover
                    %img.img-responsive.joseph
                      = link_to(image_tag(url_for(book.images), class: 'image-list'), public_book_path(book), class: 'image-list')
                    %centre
                      = link_to 'Read More', public_book_path(book), 
                        role: 'button', class: 'read-more'
      .col-md-12
        .Classic-Books
          .browse-subject
            %a Textbooks
          .scroller
            - @books.each do |book|
              .col-sm-4
                .single-item
                  .img-cover
                    %img.img-responsive.joseph
                      = link_to(image_tag(url_for(book.images), class: 'image-list'), public_book_path(book), class: 'image-list')
                  %centre
                    = link_to 'Read More', public_book_path(book), 
                      role: 'button', class: 'read-more'
      .col-md-12
        .Classic-Books
          .browse-subject
            %a Thrillers
          .scroller
            - @books.each do |book|
              .col-sm-4
                .single-item
                  .img-cover
                    %img.img-responsive.joseph
                      = link_to(image_tag(url_for(book.images), class: 'image-list'), public_book_path(book), class: 'image-list')
                    %centre
                      = link_to 'Read More', public_book_path(book), 
                        role: 'button', class: 'read-more'
      .col-md-12
        .Classic-Books
          .browse-subject
            %a Authors Alliance  MIT Press
          .scroller
            - @books.each do |book|
              .col-sm-4
                .single-item
                  .img-cover
                    %img.img-responsive.joseph
                      = link_to(image_tag(url_for(book.images), class: 'image-list'), public_book_path(book), class: 'image-list')
                  %centre
                    = link_to 'Read More', public_book_path(book), 
                      role: 'button', class: 'read-more'
      .Classic-Books
        %h2.about About the project
        .row
          .col-sm-6
            %p.c-text
              E-Library is an open, editable library catalog, building towards a web page for every book ever published.
              = succeed "." do
                %a{:href => "#"} More
            %p.s-text
              Just like Wikipedia, you can contribute new information or corrections to the catalog. You can browse by
              = succeed "," do
                %a{:href => "#"} subjects
              %a{:href => "#"} authors
              or
              %a{:href => "#"} lists
              members have created. If you love books, why not help build a library?
          .col-sm-6
            %a.underline{:href => "#"} Latest blog post
            %br/
            %span
              %a.underline.underline-small{:href => "#"} Raising Crypto for the Greater Good
              \- October 27, 2018
            %br/
            %span
              %a.underline.underline-small{:href => "#"} Google Summer of Code 2018
              \- August 12, 2018
            %br/
            %span
              %a.underline.underline-small{:href => "#"} Search Full-Text within 4M+ Books
              \- July 14, 2018
            %a{:href => ""}