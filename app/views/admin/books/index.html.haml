%h4.text.center= 'Book List'
.container
  .row 
    = link_to 'Add New Book', new_admin_book_path, remote: true, class: ' btn btn-success pull-right'
  .col-md-12
    .table-wrapper
      .table-responsive
        %table.table.table-bordered.table-stripped
          %thead
            %tr
              %th= 'Reference'
              %th= 'BookName'
              %th= 'Subject Name'
              %th= 'Action'
          %tbody
            - @books.each.with_index(1) do |book, index|
              %tr
                %td= book.id
                %td= book.book_name
                %td= book.subject_name
                %td 
                  = link_to 'Edit', edit_admin_book_path(book), remote: true, class: 'btn btn-default btn-success'
                  = link_to 'Delete', admin_book_path(book), method: :delete, class: 'btn btn-default btn-success'